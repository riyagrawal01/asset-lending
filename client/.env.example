# Vite exposes variables prefixed with VITE_ to the browser.
# In development the Vite proxy forwards /api to the server, so no override is needed.
# For production builds, set this to the deployed API URL.
# VITE_API_BASE_URL=https://your-server.example.com
